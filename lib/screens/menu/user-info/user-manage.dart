import 'package:flutter/material.dart';

class UserManage extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      body: Center(child: Text("Manage My Film"),)
    );
  }

}